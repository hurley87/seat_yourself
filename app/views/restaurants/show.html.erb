<h1><%= @restaurant.name  %></h1>
<h2>Make a reservation:</h2>

<%= form_for [@restaurant, @reservation] do |f| %>
		<%= f.label :slot %>
		<%= f.text_field :slot %>

		<%= f.label :date %>
		<%= f.date_field :date %>

		<%= f.submit("Reserve") %>
<% end %>